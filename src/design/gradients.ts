// Gradient tokens - restrained and premium gradients
export const gradients = {
  // Primary gradient (electric blue)
  primary: 'linear-gradient(135deg, #3b82f6, #2563eb)',

  // Secondary gradient (graphite)
  secondary: 'linear-gradient(135deg, #6c757d, #545b62)',

  // Subtle gradient for backgrounds
  subtle: 'linear-gradient(180deg, #ffffff, #f8f9fa)',

  // Accent gradient (could be used for buttons, etc.)
  accent: 'linear-gradient(135deg, #0ea5e9, #0284c7)',

  // Soft gradient for cards or panels
  soft: 'linear-gradient(180deg, #f8f9fa, #ffffff)',

  // Diagonal gradient for hero sections
  diagonal: 'linear-gradient(135deg, #6366f1, #8b5cf6)',

  // Radial gradient for special elements
  radial: 'radial-gradient(circle at center, #3b82f6, #1e40af)',
};

// Default export
export default gradients;